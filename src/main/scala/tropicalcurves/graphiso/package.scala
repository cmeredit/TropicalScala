package tropicalcurves

import akka.actor.typed.ActorRef
import tropicalcurves.puregraphs._

package object graphiso {
//  trait IsoMessage
//  final case class ReduceGraphsByIsomorphism[A, B](graphs: Vector[Graph[A, B]]) extends IsoMessage
//  final case class CheckIfNew[A, B](graphs: Vector[Graph[A, B]], h: Graph[A, B]) extends IsoMessage
//  final case class IsomorphismResult[A, B](g: Graph[A, B], h: Graph[A, B], isomorphic: Boolean) extends IsoMessage
}
